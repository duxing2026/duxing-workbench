# 独行工作台 · 用户规则与需求记录

> **记录时间**：2026-08-05 ｜ **项目**：独行工作台 PWA（公考备考）
> **用途**：汇总你历次对话中提出的所有需求、纠正过的错误、已确认的既定结论，作为后续迭代的"规则手册"。大部分规则已落实到 `app.js` / `index.html` / `styles.css`。

---

## 一、用户环境画像（影响所有决策）

| 项 | 内容 | 来源/说明 |
|---|---|---|
| 设备系统 | **iOS**（iPhone） | 你明确回答过环境为 iOS |
| 主要用法 | **桌面 PWA**（添加到主屏幕）为主，也会用 Safari 浏览器 | 你多次提到"桌面使用版""桌面图标" |
| 身份 | **公考（公务员考试）备考者** | 全站内容围绕公考 |
| 关键约束 | iOS 上**桌面 PWA 与 Safari 浏览器是两套独立的 localStorage 沙箱，数据互不同步** | ⚠️ 你亲自纠正过的错误认知，不要再声称"删图标数据还在"或"跨环境同步" |

---

## 二、英语模块规则

1. **Vlog 内容定位**：英语模块的 vlog 以**博主拍摄的英文 vlog** 为主（非官方/影视剪辑）。
2. **"更多推荐"更新按钮**："更多推荐"旁边要有一个 **"换一批/更新"按钮**，点一下从 B 站重新获取一批视频（`refreshMoreVlog()`，用独立 offset `duxing_vlog_more_offset`）。
3. **点击推荐视频必须同步更新信息区**：点"更多推荐"里任一视频播放时，红圈标注的**标题 / 描述 / 学习提示**区域必须同步更新，不能只换 iframe（`playVlog()` 已重写 DOM 更新）。
4. **单词来源**：单词卡片以**四六级（CET-4 / CET-6）单词**为主。
5. **遗忘曲线**：背单词与复习安排按**艾宾浩斯遗忘曲线**（5 箱 Leitner 系统，间隔 0/1/2/4/7/15 天）更新。
6. **统计可见**：可查看 **总词数 / 已掌握 / 待复习**。
7. **发音**：单词要能**发音**（Web Speech API `speechSynthesis`）。
8. **多种背词形式**：看英选义、看义选英、拼写默写、卡片复习。
9. **整体形态**：英语页面整体按**日常背单词 App** 的形式重做。
10. **Tab 固定顺序**（7 个，不可改）：
    `Vlog → 单词 → 学习 → 统计 → 听写 → 英译中 → 中译英`

---

## 三、进度持久化与跨天规则

| 规则 | 说明 |
|---|---|
| ❌ 做题进度不重置 | 时政、常识、成语背诵的做题进度**不能**在刷新或跨天后回到第 1 题，要按日期持久化。时政/常识用**「题型+日期」双键** `duxing_quiz_index_${type}_${today}`（时政 politics 与常识 common 各自独立，互不串），成语用 `duxing_idiom_index_${today}`。刷新时快照只恢复题型，题号以该持久化值为准，避免被旧快照覆盖回第 1 题。**⚠️ 持久化 index 必须在 `Nav.switchTo()` 渲染之前恢复**（曾因恢复在渲染之后，`renderQuestion()` 用初始值 0 渲染，刷新后仍回第 1 题，已于 zp 版修复）。**切换题型（`switchType`）时也要从持久化恢复 index**，否则点 tab 切换会回第 1 题（错题本除外）。 |
| ✅ 每日打卡跨天重置 | **每日打卡（每日计划里的模块打卡）必须在北京时间 00:00 跨天重置**，按用户本地时区，不是 UTC。 |
| ✅ 番茄时钟累计不清零 | 番茄时钟的**累计数据跨天不清零、持续累计**（累计总时长 / 总树数保留）；只有"今日"计数器按天重置。 |
| ⚠️ 日期必须用本地时区 | `App.today` 及所有日期键统一用 `getLocalDateStr()`（本地时区）。原 `toISOString()`（UTC）会导致中国用户跨天边界偏移 8 小时，是打卡不重置的根因。 |
| ✅ 跨天 60 秒自检 | `init()` 内置 `setInterval(60s)`：每分钟比对 `getLocalDateStr()` 与 `App.today`，一旦跨天就更新 `App.today`、跑 `DailyReset.checkNewDay()`，并**静默重渲染**依赖日期种子的每日模块（news/essay/idiom/interview/reading/exercise/formula）。解决"PWA 一直开着、过了午夜内容不更新"的问题。 |
| ✅ 成语累计打卡（断签不清零 + 可重做） | 成语背诵的"累计天数"按你要求 **断签不清零、只增不减**；答错可**重做本题直到答对**（方案 B）才计入当日完成，完成当日 25 题即 +1。UI 标签已由"连续打卡"改为"累计打卡"。`resetForNewDay` 中原本的断签归零逻辑已删除。 |
| ✅ 英语累计打卡（与成语一致） | 英语模块的"天数"统计同样改为 **累计、断签不清零、只增不减**（原逻辑是断签归 1 的"连续天数"，已改）。每天完成一次学习 +1，当日重复完成不重复计。三处标签"连续天数"→"累计天数"。 |

---

## 四、计算器键盘规则

1. 数字键盘**左下角 `,` 改为 `.`（小数点）**。
2. **右下角"确认" 改为 `/`**。
3. **"确认"移到最下方单独一整行**显示（`grid-column: span 3`）。
4. **"确认"按钮宽度要与上方 3 格行等宽**（用 `grid-column: span 3`，不要用 flex）。

---

## 五、阅读模块规则

1. **采用"内置公开书籍"方案**（你通过选项选定）。
2. **微信读书无法直接连接**——无可用公开 API，已确认不再尝试（阅读模块用内置书籍 + 手动记录 + 今日/累计统计）。

---

## 六、刷新 / PWA 更新规则

| 规则 | 说明 |
|---|---|
| 刷新不丢数据 | 刷新按钮要保持已录入状态（sessionStorage + localStorage 双写），且不丢内容。 |
| 改代码必须刷新缓存 | 改完代码必须**升级 `index.html` 里脚本的 `?v=` 版本号**并清 Cache Storage，否则桌面 PWA 看不到更新（stale cache）。 |
| 页面不能崩溃 | 初始化顺序要正确：不能在 `DB` 定义前调用它（曾因 `App` 初始化里提前 `DB.get()` 导致整页空白崩溃）。 |
| ✅ 应用内版本自检（自动更新） | 新增 `version.json`（含 `version` 字段）+ `VersionCheck` 模块：`init()` 启动即检查、每 5 分钟轮询、页面重新可见时再查；若线上 `version` 与本地 `APP_VERSION` 不同，**自动清缓存并 `location.reload(true)` 强制重载**。**效果：用户只要拿到含此机制的一版，以后每次发版都会自动更新，不再需要手动刷新。** |
| ✅ 发版两步曲 | 每次发布：① 把 `index.html` 里 `app.js?v=` / `styles.css?v=` 自增一个新 token；② 同步把 `version.json` 的 `version` 改成同一新值、把 `app.js` 顶部 `APP_VERSION` 常量改成同一新值。三者保持一致。 |
| ✅ 服务端缓存头 | 仓库 `server.js` 已改为：`html/js/css/json` 返回 `Cache-Control: no-cache`（每次重新校验），图片/图标 `max-age=86400`；并**手动剥离 URL 查询串**（`app.js?v=x` 之前会被 Node 当成文件路径导致 404，已修）。Python `http.server` / GitHub Pages 默认会剥离查询串，此修复仅针对 Node 原生服务。 |
| ⚠️ iOS 一次性跳出旧缓存 | 若设备卡在很老的版本、刷新按钮无效：从 iPhone **多任务上滑杀掉 PWA 再重开桌面图标**（iOS 会按 `no-cache` 重新拉取 index.html → 新 `?v=` 的 app.js），即可拿到含自检的新版，之后全自动。 |
| ⚠️ "刷新没反应 / 没成语"多属缓存 | 你反馈过的"按刷新没反应、成语不出来"，经无头浏览器实测应用本身正常，根因是设备跑着**旧缓存的 app.js**。已用版本自检 + `?v=` 自增 + 缓存头三重机制解决。 |

---

## 七、已确认且不再更改的既定结论

- **微信读书不可连接**（无 API）——阅读走内置书籍。
- **iOS 桌面 PWA 与 Safari 是独立 localStorage**——不做跨环境同步假设。
- **日期统一用本地时区**（`getLocalDateStr`）——已修复并应用全站。
- **做题进度持久化、不回第 1 题**——时政/常识按「题型+日期」双键、成语按日期键实现。
- **应用内版本自检自动更新**——`version.json` + `VersionCheck` 已落地，发版只需自增 `?v=` 与 `version` 字段即可自动送达 iOS PWA。

---

## 八、数据备份 / 永久保存（iOS 重点）

| 规则 | 说明 |
|---|---|
| localStorage **不永久** | 工作台数据存在设备浏览器/PWA 的 localStorage，清缓存、重装 PWA、换手机、iOS 自动清理都可能丢；且 iOS 桌面 PWA 与 Safari 是两套独立沙箱、互不同步。 |
| 永久保存 = 导出/复制 JSON | 数据管理面板提供：**导出**（下载 JSON 文件）、**复制数据到剪贴板**（粘到备忘录/云文档）、**导入**（选 JSON 恢复）。 |
| iOS 桌面 PWA 下载失效 | 独立 PWA 里 blob 下载文件常失败；"复制数据到剪贴板"按钮是为此加的 iOS 友好方案（剪贴板 API 失败会回退 `execCommand`）。最稳流程：在 **Safari** 里导出存 iCloud/文件 App，或用"复制"粘备忘录。 |
| 发版/刷新不丢数据 | 版本自检与 `App.refresh()` 只清 Cache Storage，不碰 localStorage，故升级、强制刷新都不会清空打卡/进度/累计天数。 |
| ✅ 导出代码包 | 数据管理面板新增「导出代码包 (ZIP)」：用 JSZip 将全部源码（app.js、index.html、server.js、data/*.js、styles.css、用户规则记录.md 等 24 个文件）打包为 ZIP 下载。**用途：换新会话时上传此 ZIP，即可恢复全部代码让我继续修改。** |

---

## 九、跨会话 / 新对话恢复

| 规则 | 说明 |
|---|---|
| 规则档案 | `/workspace/用户规则记录.md` 是跨会话的永久记忆来源，包含所有已确认规则。 |
| 新会话恢复流程 | ① 上传「导出代码包」ZIP（含全部源码 + 规则档案）；② 发一句"先读 `用户规则记录.md` 恢复所有规则再继续"。 |
| 沙箱重置风险 | 新会话可能重置 `/workspace`，故**必须用导出代码包功能保存源码**；规则档案也包含在代码包内。 |

---

## 十、实时新闻抓取规则（天行数据 TianAPI）

| 规则 | 说明 |
|---|---|
| API Key | 存储 key 在 `duxing_tianapi_key`（localStorage），用户在数据管理面板配置。 |
| 今日时政 | 用 `allnews/index?col=7`（国内新闻频道），按时间倒序返回最新20条。 |
| 国外热点 | 用 `allnews/index?col=8`（国际新闻频道）。原「国内外热点」改名，原「全国宏观时政」分类已删除。 |
| 云南本土时政 | **用 `areanews/index?areaname=云南`（地区新闻接口）**，从云南本地媒体源抓取，比 `allnews+word=云南` 关键词搜索精准得多。⚠️ 需要用户在天行数据单独申请「地区新闻」接口（[apiview/154](https://www.tianapi.com/apiview/154)，免费100次/天）。**未申请时**：显示友好提示（橙色提示框 + 申请链接），回退到内置数据。**已申请时**：自动使用 areanews 接口，返回本地时政新闻。 |
| 缓存 | 各分类独立缓存24h（`duxing_live_${sectionKey}_cache`），避免频繁调用 API。 |
| 回退 | API 失败时（网络错误、次数用尽、未申请等）自动回退到内置静态数据，并显示失败原因。 |
| 字段兼容 | 同时兼容 `data.result.newslist` 和 `data.result.list` 两种返回格式。 |
| 日期格式 | `ctime` 提取前10位并用正则格式化为 `YYYY年MM月DD日`（不用 `replace(/-/g,'年')`，那会格式错乱）。 |

---

> 维护建议：后续每次你提出新需求或纠正，我都追加到对应章节并更新日期，保持这份手册与代码行为一致。
